<!-- begin client-logos -->
<% _.each(data, function(logo, index) {

	if (index % 3 === 0) {
		%>
		<div class="row">
	<% } %>

	<div class="four column">
		<%- image_tag(logo.href, {
			class: 'u-img-responsive u-center-block client-logo'
			, alt: logo.alt
		}) %>
	</div>

	<%
	if (index % 3 !== 0 && (index+1) % 3 === 0) {
		%>
		</div>
	<% }

}) %>
<!-- / client-logos -->
