<!-- begin person -->
<div class="row person">

	<div class="columns lg-two lg-offset-by-one
		md-two lg-offset-by-three
		sm-six sm-offset-by-zero
		xs-six">

		<%- image_tag(data.image.href, {
			class: 'u-img-responsive u-pull-left profile-photo'
			, alt: data.name
			, width: data.image.width || ''
			, height: data.image.height || ''
		}) %>

	</div>
	<div class="columns lg-seven md-eight sm-twelve xs-twelve">
		<% if (data.name) { %>

		<h4><%- data.name %>

			<% if (data.title) { %>
			<br><small><%- data.title %></small>
			<% } %>

		</h4>

		<% } %>

		<%- data.bio %>
	</div>

</div>
<!-- /  person -->
