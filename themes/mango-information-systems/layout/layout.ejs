<!DOCTYPE html>
<html>
	<%- partial('_partial/head') %>
	<body>
		<%- body %>
			
		<%  if (page.scripts) {
				page.scripts.forEach(function(scriptFile) {%>
				<%- js('js/' + scriptFile) %>
			<%  })
			}
		%>
	</body>
</html>
